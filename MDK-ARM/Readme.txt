硬件uitra_core_20260409 (�������հ�)
软件版本V1.1
更新202600427;
1.修改调试串口输出位RTT

更新20260428;
1.气泵采用新泵，驱动方式改为PWM调速

更新20260429;
1.调整错误弹窗，错误状态编码与错误弹窗状态一致
   （1）、错误修正PCB与原理图信号：紧急开关与气压过压IO状态信号脚位对调。
2.问题：RGB氛围灯铜芯线接触不良，导致I2C通信延时影响系统其他任务。
    解决方案：增加RGB氛围灯I2C通信错误计数处理，连续错误3次（300ms超时），关闭RGB更新功能，等待重启清除错误计数。
3、当前硬件版本：
（1）、蠕动泵信号高有效（下一版本修正电路后变低有效，和过压信号，通过与门进行控制）
（2）、气泵低有效     （下一版本修正电路后变高有效，和过压信号，通过或非门进行使能控制）

4、出现过任务内存占用>90% 的任务，有溢出风险，继续寻找定位，找出改任务。
PTC加热功能

更新20260506;
1、电源GWB3200W,休眠唤醒时间管理:
    判定电源输出状态，关闭后重新打开，发送开机指令后需等待大约4秒，开机状态重新更新。
2、脉冲宽度改为TIM2外部触发模式，脉冲频率改为由TIM5控制。

更新20260507;
1、脉冲触发方式更改，测量脉宽；
2、 已获得两组脉宽测量数据，明天继续数据分析，调整脉宽，争取精度±2us;

更新20260508;
1、脉宽计数IO口与脚踏IO口 均采用外部中断线EXTI15，冲突；
   方案，脉宽计数IO取消外部中断线，采用TIM2_ETR方式控制TIM2脉宽计数。
   
更新20260509;
1、脉宽计数IO口与脚踏IO口 均采用外部中断线EXTI15，冲突；
   方案，脉宽计数IO取消外部中断线，采用TIM2_ETR方式控制TIM2脉宽计数。
    未加入动态脉宽调整功能脉宽±2us.
    2、关闭打印,EMC摸底使用版本,关闭蜂鸣器工作心跳提示音。
    3、检查iwdg喂狗操作，有超时代码，影响喂狗！！！

更新202600511;
1、问题：脉冲定时器TIM2，采用向上计数模式，首脉冲异常过宽！！！
   需要改用向下计数模式。解决首脉冲宽度问题

更新20260512;
1、问题：脉冲定时器TIM2，首脉冲异常过宽！！！    
   原因：在OC影子寄存器值更新之前、输入捕获库函数“HAL_TIM_OC_Start_IT(&htim2,TIM_CHANNEL_2);”提前开启了定时器计数，导致比较值更新滞后。
         同时，首脉冲宽度为固定值，没有动态调整，每次启用OC功能就需要重新配置通道脉冲初始值。
   解决方式:（1）、更改向下计数方式
           （2）、将OC影子寄存器值更新放在定时器启动之前，输入捕获只使能中断，不开启计数器。
           （3）、为方便使用IC功能动态调整脉宽，设定在下一个更新事件后改变（output compare preload ==ENABLE）,、
                  这导致首脉冲OC比较值为固定初始值，因此首脉冲采用立即更新的方式。
                  代码如下：
                  {
                  __HAL_TIM_DISABLE_OCxPRELOAD(&htim2, TIM_CHANNEL_2);//先立即更新
	               __HAL_TIM_SetCompare(&htim2,TIM_CHANNEL_2,timeLoad100ns-1); //MAX pulse 500us
	               __HAL_TIM_ENABLE_OCxPRELOAD(&htim2, TIM_CHANNEL_2);//wait next update event
                  }
            提醒：如需脉宽调整在当前脉冲，可采用方案如下：
                  (A)、计数方式改为向上计数，
                  (B)、脉冲OC值立即更新“ __HAL_TIM_DISABLE_OCxPRELOAD(&htim2, TIM_CHANNEL_2);”
                  (C)、如此可实现在当前脉冲动态调整，本软件版本采用下一个周期更新即可。！！
                    
   

更新20260514;
1、问题：预燃等待时间内部采用了“HAL_DELAY()”导致任务没有切换，喂狗超时。
   解决方式：将“HAL_DELAY()”更改为“osDelay()”，避免高优先级任务独占cpu;
   
更新20260515;
1、开机关闭气泵。
2、 1小时200mJ,60Hz连续输出测试。
      原始数据分析：
         （1）、连续运行15分钟后，能量约10mJ的衰减5%；
         （2）30分钟后有约20mj的衰减；10%的范围；
         （3）45分钟后有约40mj的衰减；20%的范围。
      原因（待验证）：1、是电源输出时间变长，温度升高，电源输出效率变化。
            2、也有可能由于测温只测到冷却液温度，激光器内部热功率累积造成温差，输出效率波动。			
			
			更新202600522;
		1、增加Vdac根据反馈光电管值动态调整的功能，暂未测试启用。
		
		
更新20260603;		
1、脉冲能量ADC增加卡尔曼滤波。

更新20260618;		
1、散热风扇增加风量，比压缩机多1000转以带走多余热量。
2、压缩机控制器与压缩机不匹配，控制参数按照压缩机参数配置。
	/*压缩机型号：6MS020ZBB41
	*额定转速：4500rpm
	*/
	//控制器型号:GE2117

/**********************/
***硬件uitra_core_20260409硬件版本遗留问题
（1）、蠕动泵信号高有效（下一版本修正电路后变低有效，和过压信号，通过与门进行控制）
（2）、气泵低有效     （下一版本修正电路后变低有效，和过压信号，通过或非门进行使能控制）
（3）、未做激光长时间工作测试（最高能量、1小时以上）。
（4）、Vdac低于1.8V ，光电信号触发脉冲无法触发。将电流过低调整放大倍数。

（5）、202600603赛宝实验室EMC摸底测试：
		问题1：200mJ频率60Hz连续运行40分钟左右设备断电。预估原因控制芯片散热不良温度过高。
		问题2：线脚踏一段时间后失效。可能受到问题1影响。
   已解决：
（1）、首脉冲脉宽异常(202605142已解决)
（2）、检查iwdg喂狗操作，有超时代码，影响2s喂狗！！！(20260514已解决)

/**************************/